<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Xác nhận Thanh toán - Bàn <%= MaBan %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #e9ecef;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .payment-container {
            max-width: 600px;
            width: 100%;
            background: white;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            padding: 40px;
            text-align: center;
        }

        h2 {
            font-weight: 700;
            color: #007bff;
            margin-bottom: 25px;
        }

        .summary p {
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .total-amount {
            font-size: 2.2rem;
            font-weight: bold;
            color: #dc3545;
            margin: 20px 0 30px;
            padding: 10px;
            border: 2px dashed #dc3545;
            border-radius: 8px;
            display: inline-block;
        }

        .form-select {
            max-width: 300px;
            margin: 15px auto;
            border-radius: 8px;
        }

        .btn-confirm {
            padding: 12px 40px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 8px;
        }
    </style>
</head>

<body>
    <div class="payment-container">
        <h2>Xác Nhận Thanh Toán</h2>
        <div class="summary">
            <p><strong>Bàn:</strong> <%= MaBan %></p>
            <p><strong>Nhân viên:</strong> <%= TenNhanVien %></p>
            <p><strong>Mã Order:</strong> <%= MaOderList.join(', ') %></p>
        </div>

        <div class="total-amount">
            <%= TongTien.toLocaleString() %> ₫
        </div>

        <form action="/order/pay" method="POST">
            <input type="hidden" name="MaBan" value="<%= MaBan %>">
            <input type="hidden" name="TongTien" value="<%= TongTien %>">
            <input type="hidden" name="MaOderList" value="<%= MaOderList.join(',') %>">

            <select name="PhuongThuc" class="form-select" required>
                <option value="">-- Chọn Phương thức thanh toán --</option>
                <option value="Tien mat">💰 Tiền mặt</option>
                <option value="The">💳 Thẻ (VISA/Master)</option>
                <option value="Vi dien tu">📱 Ví điện tử (Momo/ZaloPay)</option>
            </select>

            <button type="submit" class="btn btn-success btn-confirm mt-4">HOÀN TẤT THANH TOÁN</button>
        </form>
        
        <a href="/order/bill/<%= MaBan %>" class="btn btn-secondary mt-3"> Quay lại Hóa đơn</a>
    </div>
</body>

</html>