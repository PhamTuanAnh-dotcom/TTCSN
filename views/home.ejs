<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Nhà hàng siêu cấp VIP PRO</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family: Arial, sans-serif; }

    body {
      background: #f5f6fa;
      min-height: 100vh;
    }

    /* Header */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: #fff;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    header h1 {
      font-size: 24px;
      color: #2c3e50;
      font-weight: bold;
    }

    header .nav-links a {
      text-decoration: none;
      font-weight: bold;
      color: #2c3e50;
      margin-left: 20px;
      padding: 8px 15px;
      border-radius: 5px;
      transition: 0.2s;
    }

    header .nav-links a:hover {
      background: #2c3e50;
      color: #fff;
    }

    /* Main content */
    main {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 50px 20px;
      text-align: center;
    }

    main h2 {
      font-size: 28px;
      color: #34495e;
      margin-bottom: 15px;
    }

    main p {
      font-size: 18px;
      color: #555;
      margin-bottom: 30px;
    }

    .btn-group a {
      padding: 12px 25px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      margin: 0 10px;
      transition: 0.2s;
    }

    .btn-login {
      background: #007bff;
      color: #fff;
    }
    .btn-login:hover {
      background: #0056b3;
    }

    .btn-register {
      background: #28a745;
      color: #fff;
    }
    .btn-register:hover {
      background: #218838;
    }

    /* Popup login */
    #loginOverlay {
      display: none;
      position: fixed;
      top:0;
      left:0;
      width:100%;
      height:100%;
      background: rgba(0,0,0,0.6);
      justify-content: center;
      align-items: center;
      z-index: 200;
      animation: fadeOverlay 0.3s ease forwards;
    }

    @keyframes fadeOverlay {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    #loginBox {
      background: #fff;
      padding: 40px 25px 25px 25px;
      border-radius: 12px;
      width: 350px;
      box-shadow: 0 0 15px rgba(0,0,0,0.3);
      text-align: center;
      position: relative;
      transform: translateY(-30px);
      opacity: 0;
      animation: slideFadeIn 0.4s ease forwards;
    }

    @keyframes slideFadeIn {
      from { transform: translateY(-30px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    #closeBtn {
      position: absolute;
      top: 10px;
      right: 10px;
      background: #e74c3c;
      border: none;
      color: #fff;
      font-size: 18px;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      cursor: pointer;
      line-height: 0;
      transition: transform 0.2s ease, background 0.2s ease;
    }

    #closeBtn:hover {
      background: #c0392b;
      transform: scale(1.2);
    }

    #loginBox input {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 16px;
    }

    #loginBox button[type="submit"] {
      width: 100%;
      padding: 12px;
      border: none;
      background: #007bff;
      color: #fff;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 10px;
      transition: background 0.2s ease;
    }

    #loginBox button[type="submit"]:hover {
      background: #0056b3;
    }

    @media (max-width: 500px) {
      #loginBox { width: 90%; padding: 20px; }
      header h1 { font-size: 20px; }
      main h2 { font-size: 22px; }
      main p { font-size: 16px; }
    }
  </style>
</head>
<body>
  <header>
    <h1>NHÀ HÀNG SIÊU CẤP VIP PRO</h1>
    <div class="nav-links">
      <a href="#" class="btn-login" id="btnLoginHeader"><i class="fa fa-user"></i> Đăng nhập</a>
    </div>
  </header>

  <main>
    <h2>Chào mừng đến với hệ thống quản lý nhà hàng</h2>
    <p>Trải nghiệm quản lý đơn hàng, bàn ăn và nhân viên dễ dàng và trực quan.</p>
    <div class="btn-group">
    </div>
  </main>

  <!-- Popup login -->
 <div id="loginOverlay">
  <div id="loginBox">
    <button id="closeBtn">&times;</button>
    <i class="fa fa-user-circle" style="font-size:50px;color:#007bff;"></i>
    <h2>Đăng nhập hệ thống</h2>

    <% if (typeof error !== 'undefined' && error) { %>
      <div style="color: #e74c3c; background: #fdf2f2; padding: 10px; border-radius: 5px; margin-top: 10px; font-weight: bold; border: 1px solid #fab1a0;">
        <i class="fa fa-exclamation-triangle"></i> <%= error %>
      </div>
    <% } %>

    <form action="/auth/login" method="POST">
      <input type="text" name="TaiKhoan" placeholder="Tên đăng nhập" required>
      <input type="password" name="MatKhau" placeholder="Mật khẩu" required>
      <button type="submit">Đăng nhập</button>
    </form>
  </div>
</div>

  <script>
    const btnLoginHeader = document.getElementById("btnLoginHeader");
    const overlay = document.getElementById("loginOverlay");
    const closeBtn = document.getElementById("closeBtn");

    function showLogin() {
      overlay.style.display = "flex";
      const box = document.getElementById("loginBox");
      box.style.animation = "none";
      void box.offsetWidth; // reflow
      box.style.animation = "slideFadeIn 0.4s ease forwards";
    }

    btnLoginHeader.onclick  = function(e) {
      e.preventDefault();
      showLogin();
    };

    closeBtn.onclick = function() { overlay.style.display = "none"; };
    overlay.onclick = function(e) { if (e.target === overlay) overlay.style.display = "none"; };
    window.onload = function() {
    <% if (typeof error !== 'undefined' && error) { %>
      showLogin();
    <% } %>
  };
  </script>
</body>
</html>
