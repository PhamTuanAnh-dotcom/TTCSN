<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Trang bếp - Nhà hàng VIP PRO</title>
  <link rel="stylesheet" href="/css/home_bep.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<style>
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #e0e0e0;
}
.container {
  display: flex;
  min-height: 100vh;
}

/* Sidebar */
.sidebar {
  width: 220px;
  background: #2c3e50;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

/* Logo */
.logo {
  padding: 20px 0;
  text-align: center;
  color: #fff;
  font-size: 18px;
  font-weight: bold;
  border-bottom: 1px solid rgba(255,255,255,0.1);
}

/* Menu */
.menu {
  list-style: none;
  padding: 10px 0;
  margin: 0;
}

.menu li {
  margin: 4px 0;
}

.menu li a {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 20px;
  color: #ecf0f1;
  text-decoration: none;
  font-size: 14px;
  border-left: 4px solid transparent;
  transition: all 0.25s ease;
}

.menu li a i {
  width: 18px;
  text-align: center;
}

/* Hover */
.menu li a:hover {
  background: #34495e;
  border-left: 4px solid #f1c40f;
  color: #f1c40f;
}

/* Active */
.menu li.active a {
  background: #1abc9c;
  border-left: 4px solid #16a085;
  color: #fff;
  font-weight: bold;
}

/* Logout */
.logout {
  margin-top: auto;
  padding: 15px;
  text-align: center;
  border-top: 1px solid rgba(255,255,255,0.1);
}

.logout a {
  color: #e74c3c;
  text-decoration: none;
  font-weight: bold;
}

.logout a:hover {
  text-decoration: underline;
}

/* Main */
.main {
  flex: 1;
  padding: 20px;
}
.main h1 {
  text-align: center;
  margin-bottom: 25px;
}

/* Order card */
.ban-list {
  display: flex;
  flex-direction: column;
  gap: 15px;
  align-items: center;
}
.ban-item {
  background: #fff;
  width: 80%;
  padding: 15px 20px;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.15);
}
.ban-item h3 {
  margin: 0 0 5px;
}
.ban-item p {
  margin: 3px 0;
}

/* Item */
.mon-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 4px 0;
}
.mon-item em {
  font-size: 12px;
  color: #777;
}
.cancel-item-btn {
  border: none;
  background: transparent;
  color: #e74c3c;
  font-size: 16px;
  cursor: pointer;
}
.cancel-item-btn:hover {
  color: #c0392b;
}

/* Buttons */
.action-btns {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 10px;
}
.done-btn {
  background: #27ae60;
  color: #fff;
  border: none;
  padding: 6px 14px;
  border-radius: 5px;
  cursor: pointer;
}
.done-btn:hover {
  background: #2ecc71;
}
.cancel-btn {
  background: #e74c3c;
  color: #fff;
  border: none;
  padding: 6px 14px;
  border-radius: 5px;
  cursor: pointer;
}
.cancel-btn:hover {
  background: #ff6b5a;
}
</style>

<body>
<div class="container">
  <!-- Sidebar -->
  <aside class="sidebar">
    <div class="logo">
      <h3>🍳 BẾP</h3>
    </div>
    <ul class="menu">
  <li class="active">
    <a href="/home_bep">
      <i class="fa-solid fa-house"></i> Trang chủ
    </a>
  </li>
  <li>
    <a href="#">
      <i class="fa-solid fa-user"></i> Thông tin cá nhân
    </a>
  </li>
</ul>

    <div class="logout">
      <a href="/auth/logout">Đăng xuất</a>
    </div>
  </aside>

  <!-- Main -->
  <main class="main">
    <h1>NHÀ HÀNG SIÊU CẤP VIP PRO</h1>

    <div class="ban-list">
      <% if (orders && orders.length > 0) { %>
        <% orders.forEach(order => { %>
          <div class="ban-item">
            <h3>Bàn <%= order.MaBan %></h3>
            <p><strong>Mã Order:</strong> <%= order.MaOder %></p>

            <ul>
              <% order.monAnList.forEach(mon => { %>
                <li class="mon-item">
                  <span>
                    <%= mon.TenMon %> - <%= mon.SoLuong %> phần
                    <% if (mon.GiChu) { %>
                      <em>(<%= mon.GiChu %>)</em>
                    <% } %>
                  </span>
                  <button class="cancel-item-btn"
                    onclick="huyItem('<%= order.MaOder %>','<%= mon.MaMon %>')">
                    ✖
                  </button>
                </li>
              <% }) %>
            </ul>

            <div class="action-btns">
              <button class="done-btn"
                onclick="hoanThanh('<%= order.MaOder %>')">
                Hoàn thành
              </button>

              <button class="cancel-btn"
                onclick="huyOrder('<%= order.MaOder %>')">
                Hủy Order
              </button>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>Hiện chưa có đơn hàng nào cần chế biến.</p>
      <% } %>
    </div>
  </main>
</div>

<script>
function hoanThanh(maOder) {
  if (confirm(`Xác nhận hoàn thành order ${maOder}?`)) {
    fetch(`/home_bep/hoan-thanh/${maOder}`, { method: "POST" })
      .then(res => res.text())
      .then(msg => {
        alert(msg);
        location.reload();
      });
  }
}

function huyOrder(maOder) {
  if (confirm(`❌ Xác nhận HỦY order ${maOder}?`)) {
    fetch(`/home_bep/cancel-order/${maOder}`, { method: "POST" })
      .then(res => res.text())
      .then(msg => {
        alert(msg);
        location.reload();
      });
  }
}

function huyItem(maOder, maMon) {
  if (confirm("Hủy món này?")) {
    fetch("/home_bep/cancel-item", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ MaOder: maOder, MaMon: maMon })
    })
      .then(res => res.text())
      .then(msg => {
        alert(msg);
        location.reload();
      });
  }
}

// ✅ Dữ liệu quản lý từ server (Node truyền vào)
   const chef = {
  hoten: "<%= chef?.HoTen || '' %>",
  sdt: "<%= chef?.SDT || '' %>",
  gmail: "<%= chef?.Gmail || '' %>",
  cccd: "<%= chef?.CCCD || '' %>",
};
  const contentArea = document.querySelector('.main');
  const sidebarLinks = document.querySelectorAll('.sidebar a');

  sidebarLinks.forEach(link => {
    link.addEventListener('click', e => {
      const text = link.textContent.trim();
       if ( text.includes('Đăng xuất')) return;
    e.preventDefault();

      // Bỏ active cũ và set active mới
      sidebarLinks.forEach(l => l.classList.remove('active'));
      link.classList.add('active');
      
      if (text.includes('Trang chủ')) {
    location.reload();  // tải lại trang gốc
}
       if (text.includes('Thông tin cá nhân')) {
        // ✅ Hiển thị thông tin từ DB
        contentArea.innerHTML = `
          <header>THÔNG TIN CÁ NHÂN</header>
          <div class="p-4">
            <h5>👤 Thông tin nhân viên</h5>
            <hr>
            <p><strong>Họ tên:</strong> ${chef.hoten}</p>
            <p><strong>Số điện thoại:</strong> ${chef.sdt}</p>
            <p><strong>Email:</strong> ${chef.gmail}</p>
            <p><strong>CCCD:</strong> ${chef.cccd}</p>
            <button class="btn btn-primary edit-info">Chỉnh sửa</button>
          </div>
        `;
      }
    });
  });
  const profilePanel = document.getElementById("profilePanel");
const closePanel = document.getElementById("closePanel");

// Khi bấm vào “Thông tin cá nhân”
document.querySelector('.sidebar a:nth-child(2)').addEventListener('click', e => {
  e.preventDefault();

  // Đợi một chút để DOM cập nhật xong
  setTimeout(() => {
    const editBtn = document.querySelector('.edit-info');
    if (editBtn) {
      editBtn.addEventListener('click', e2 => {
        e2.preventDefault();
        profilePanel.classList.add("active");
      });
    }
  }, 100);
});
// Khi bấm nút X
closePanel.addEventListener('click', () => {
  profilePanel.classList.remove("active");
});
  </script>
</body>
</html>
