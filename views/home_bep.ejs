<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Trang bếp - Nhà hàng VIP PRO</title>
  <link rel="stylesheet" href="/css/home_bep.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<style>
  body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #e0e0e0; /* nền tổng thể sáng xám */
}

.container {
  display: flex;
  min-height: 100vh;
}

/* Sidebar */
.sidebar {
  width: 200px;
  background: #2c3e50;
  display: flex;
  flex-direction: column;
  padding: 20px 0;
}

.logo h3 {
  text-align: center;
  margin: 0 0 30px 0;
}

.menu {
  list-style: none;
  padding: 0;
  margin: 0;
}

.menu li {
  padding: 8px 20px;
  display: flex;
  align-items: center;
  margin: 0;
}

.menu li.active {
  background: #d0d0d0;
}

.menu li i {
  margin-right: 8px;
}

.menu li a {
  text-decoration: none;
  color: white;
  flex: 1;
}

.logout {
  text-align: center;
  padding: 10px;
}

.logout a {
  color: #e74c3c;
  text-decoration: none;
  font-weight: bold;
}

/* Main content */
.main {
  flex: 1;
  padding: 20px;
  overflow-y: auto;
}

.main h1 {
  text-align: center;
  margin-bottom: 30px;
  font-size: 24px;
  color: #333;
}

/* Danh sách bàn */
.ban-list {
  display: flex;
  flex-direction: column;
  gap: 15px;
  align-items: center;
}

.ban-item {
  background: #fff;
  border-radius: 8px;
  width: 80%;
  padding: 15px 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.ban-item h3 {
  margin: 0;
  font-size: 18px;
  color: #333;
}

.ban-item p {
  margin: 0;
  font-size: 14px;
  color: #555;
}

.ban-item ul {
  list-style: none;
  padding-left: 15px;
  margin: 0;
}

.ban-item ul li {
  padding: 3px 0;
  font-size: 14px;
  color: #555;
}

/* Nút hoàn thành */
.done-btn {
  align-self: flex-end;
  background: #27ae60;
  color: #fff;
  border: none;
  padding: 6px 12px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 14px;
}

.done-btn:hover {
  background: #2ecc71;
}

</style>
<body>
  <div class="container">
    <!-- Sidebar -->
    <aside class="sidebar">
      <div class="logo">
        <h3>🍳 BẾP</h3>
      </div>
      <ul class="menu">
        <li class="active"><i class="fa-solid fa-house"></i> <a href="home_bep.ejs">Trang chủ</a></li>
        <li><i class="fa-solid fa-user"></i> <a href="#">Thông tin cá nhân</a></li>
      </ul>
      <div class="logout">
        <a href="/auth/logout">Đăng xuất</a>
      </div>
    </aside>

    <!-- Main content -->
    <main class="main">
      <h1>NHÀ HÀNG SIÊU CẤP VIP PRO</h1>

      <div class="ban-list">
        <% if (orders && orders.length > 0) { %>
          <% orders.forEach(order => { %>
            <div class="ban-item">
              <h3>Bàn <%= order.MaBan %></h3>
              <p><strong>Mã Order:</strong> <%= order.MaOder %></p>
              <ul>
                <% order.monAnList.forEach(mon => { %>
                  <li>
                    <%= mon.TenMon %> - <%= mon.SoLuong %> phần
                  </li>
                  <li>
                    Ghi chú: <%= mon.GiChu || 'Không có' %>
                  </li>
                <% }) %>
              </ul>
              <button class="done-btn" onclick="hoanThanh('<%= order.MaOder %>')">Hoàn thành</button>
            </div>
          <% }) %>
        <% } else { %>
          <p>Hiện chưa có đơn hàng nào cần chế biến.</p>
        <% } %>
      </div>
    </main>
  </div>

  <script>
    function hoanThanh(maOder) {
  if (confirm(`Xác nhận hoàn thành order ${maOder}?`)) {
    fetch(`/home_bep/hoan-thanh/${maOder}`, { method: "POST" })
      .then(res => res.text())
      .then(msg => {
        alert(msg);        
        location.reload(); 
      })
      .catch(err => console.error(err));
  }
}
// ✅ Dữ liệu quản lý từ server (Node truyền vào)
   const chef = {
  hoten: "<%= chef?.HoTen || '' %>",
  sdt: "<%= chef?.SDT || '' %>",
  gmail: "<%= chef?.Gmail || '' %>",
  cccd: "<%= chef?.CCCD || '' %>",
};
  const contentArea = document.querySelector('.main');
  const sidebarLinks = document.querySelectorAll('.sidebar a');

  sidebarLinks.forEach(link => {
    link.addEventListener('click', e => {
      const text = link.textContent.trim();
       if ( text.includes('Đăng xuất')) return;
    e.preventDefault();

      // Bỏ active cũ và set active mới
      sidebarLinks.forEach(l => l.classList.remove('active'));
      link.classList.add('active');
      
      if (text.includes('Trang chủ')) {
    location.reload();  // tải lại trang gốc
}
       if (text.includes('Thông tin cá nhân')) {
        // ✅ Hiển thị thông tin từ DB
        contentArea.innerHTML = `
          <header>THÔNG TIN CÁ NHÂN</header>
          <div class="p-4">
            <h5>👤 Thông tin nhân viên</h5>
            <hr>
            <p><strong>Họ tên:</strong> ${chef.hoten}</p>
            <p><strong>Số điện thoại:</strong> ${chef.sdt}</p>
            <p><strong>Email:</strong> ${chef.gmail}</p>
            <p><strong>CCCD:</strong> ${chef.cccd}</p>
            <button class="btn btn-primary edit-info">Chỉnh sửa</button>
          </div>
        `;
      }
    });
  });
  const profilePanel = document.getElementById("profilePanel");
const closePanel = document.getElementById("closePanel");

// Khi bấm vào “Thông tin cá nhân”
document.querySelector('.sidebar a:nth-child(2)').addEventListener('click', e => {
  e.preventDefault();

  // Đợi một chút để DOM cập nhật xong
  setTimeout(() => {
    const editBtn = document.querySelector('.edit-info');
    if (editBtn) {
      editBtn.addEventListener('click', e2 => {
        e2.preventDefault();
        profilePanel.classList.add("active");
      });
    }
  }, 100);
});
// Khi bấm nút X
closePanel.addEventListener('click', () => {
  profilePanel.classList.remove("active");
});
  </script>
</body>
</html>
